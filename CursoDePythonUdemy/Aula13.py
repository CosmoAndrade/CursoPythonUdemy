# dict -> Dicionários

# keys() # Retorna a chave
# values() # Retorna os valores
# items() # Retorna os itens

cores = {
    'A': 'Verde',
    'B': 'Vermelho',
    'C': 'Amarelo'
    }
print (cores)    
print (cores['A'])
cores ['D'] = 'Preto'
print (cores)
print (cores.keys())
print (cores.values())
print (cores.items())

